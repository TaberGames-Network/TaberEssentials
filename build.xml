<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project basedir="." default="build" name="CFEssentials">

    <property name="bukkit-location" value="/craftbukkit.jar"/>
    <property name="output-jar" value="${ant.project.name}.jar"/>

    <property name="debuglevel" value="source,lines"/>

    <path id="classpath">
        <pathelement location="bin"/>
        <pathelement location="${bukkit-location}"/>
    </path>

    <target name="init">
        <mkdir dir="bin"/>
        <copy file="plugin.yml" todir="bin" />
    </target>

    <target name="clean">
        <delete dir="bin"/>
        <delete dir="dist"/>
    </target>

    <target depends="init" name="build">
        <echo message="${ant.project.name}: ${ant.file}"/>
        <javac debug="true" debuglevel="${debuglevel}" destdir="bin">
            <src path="src"/>
            <classpath refid="classpath"/>
        </javac>
    </target>

    <target name="jar">
        <mkdir dir="dist"/>
        <jar destfile="dist/${output-jar}" basedir="bin" />
    </target>

    <target name="dist" depends="clean,build,jar">
    </target>
</project>